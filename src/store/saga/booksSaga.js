
    // const responses = yield all(booksId.map(elem => {
    //     return call(fetch, `https://openlibrary.org/books/${elem}.json`)
    // }))
    // const books = yield all(responses.map(elem => {
    //     return call([elem, elem.json])
    // }
    // ))
    // console.log(books)